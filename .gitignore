# dependencies
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
coverage/

# next.js
.next/
out/
build/
next-env.d.ts

# env — never commit keys
.env
.env*.local

# vercel
.vercel

# supabase local
supabase/.temp/
supabase/.branches

# typescript
*.tsbuildinfo

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.idea/
.vscode/*
!.vscode/extensions.json

.vercel
.env*

# The dev server's log. `npm run dev` must be started detached with its output
# redirected to a file — an attached stdout that stops being read kills Next on
# an uncaught EPIPE, and the process then spins instead of exiting while every
# request hangs. See CLAUDE.md.
dev.log
